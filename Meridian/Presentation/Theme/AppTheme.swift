import Foundation
import CoreGraphics

// MARK: - AppTheme
/// A namespace for the app's design system constants.
enum AppTheme {
    
    /// Spacing constants used for layout.
    enum Spacing {
        static let small: CGFloat = 4
        static let medium: CGFloat = 8
        static let large: CGFloat = 16
        static let xlarge: CGFloat = 32
    }
    
    /// Corner radius constants for UI elements.
    enum CornerRadius {
        static let standard: CGFloat = 12
    }
    
    /// Standard sizes for icons.
    enum IconSize {
        static let medium: CGFloat = 50
        static let large: CGFloat = 100
    }
    
    /// Standard font weights use in the app
    enum FontWeight {
    }
    
    /// Standard colors for the app.
    enum Colors {
    }
}
